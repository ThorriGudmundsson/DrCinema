import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  listContainer: {
    // flex: 1,
    // flexDirection: 'row',
    // backgroundColor: '#3D3C3A',
    // flexGrow: 1,
    paddingBottom: 550,
  },
});
